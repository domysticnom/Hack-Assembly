//Proposition 5: The operating system is installed and the disk is not pertitioned
//p: The Apache software is installed
//q: The PHP is installed

CHIP Proposition15{
     IN p, q;
     OUT out;

     PARTS:

     Xor(a =p, b= q, out = out);
     
     
     
  
     
     

}